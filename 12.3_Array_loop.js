//Looping over arrays
let names = ['spiderman', 'batman', 'hulk', 'shaktimaan', 'ironman'];
for(let idx =0; idx<names.length; idx++)
{
    console.log(names[idx]);
}

// for of
for (let name of names)
{
    console.log(names);
}